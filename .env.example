# Copy to .env and fill in. Never commit a real key.
# Provider: OpenAI. One eval run (~24 questions) is typically well under USD 0.05 with gpt-4o-mini.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=30
